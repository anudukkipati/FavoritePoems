<h3>Add a New Poem</h3>

<%= render partial: "layouts/errors", locals: {object: @poem} %>
<%= form_for(@poem) do |f|%>
<%= f.label :title %>
  <%= f.text_field :title %>
  <br><br>
  <br><br>
  <%= f.label :content %>
  <%= f.text_area :content %>
  <br><br>
  <br><br>

  <%= f.submit %>

<% end %>