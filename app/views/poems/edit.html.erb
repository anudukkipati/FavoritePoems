<%= render partial: "layouts/errors", locals: {object: @poem} %>
<h3>Edit <%= @poem.title%></h3>

<%= render partial: 'form' %>

<p class="poem-edit"><%= link_to "Go Back to the Poem", poem_path(@poem), class: "btn btn-warning" %><p>